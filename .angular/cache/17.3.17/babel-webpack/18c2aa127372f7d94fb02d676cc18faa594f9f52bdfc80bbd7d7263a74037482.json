{"ast":null,"code":"import { BehaviorSubject, map, shareReplay } from 'rxjs';\nimport { Client } from '@stomp/stompjs';\nimport SockJS from 'sockjs-client';\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"@angular/common/http\";\nexport let AircraftService = /*#__PURE__*/(() => {\n  class AircraftService {\n    constructor(http) {\n      this.http = http;\n      this.activeAlertsSubject = new BehaviorSubject(0);\n      this.activeAlerts$ = this.activeAlertsSubject.asObservable();\n      // =========================\n      // 📦 STATE\n      // =========================\n      this.aircraftSubject = new BehaviorSubject([]);\n      this.alertsSubject = new BehaviorSubject([]);\n      this.trafficHistorySubject = new BehaviorSubject([0]);\n      // ✅ STATS SUBJECT\n      this.statsSubject = new BehaviorSubject({\n        aircraftCount: 0,\n        averageSpeed: 0,\n        alertCount: 0\n      });\n      this.aircraft$ = this.aircraftSubject.asObservable();\n      this.alerts$ = this.alertsSubject.asObservable();\n      this.trafficHistory$ = this.trafficHistorySubject.asObservable();\n      // ✅ DASHBOARD STATS\n      this.stats$ = this.statsSubject.asObservable();\n      // ✅ COUNTRY ANALYTICS\n      this.trafficByCountry$ = this.aircraft$.pipe(map(aircraft => this.buildCountryTraffic(aircraft)), shareReplay(1));\n      this.loadInitialAlerts();\n      this.connectWebSocket();\n    }\n    connectWebSocket() {\n      this.stompClient = new Client({\n        webSocketFactory: () => new SockJS('http://localhost:8084/ws'),\n        reconnectDelay: 3000,\n        debug: msg => console.log('[WS]', msg)\n      });\n      this.stompClient.onConnect = () => {\n        console.log('✅ WebSocket connecté');\n        // =========================\n        // ✈️ AIRCRAFT STREAM\n        // =========================\n        this.stompClient.subscribe('/topic/processed', message => {\n          const aircraft = JSON.parse(message.body);\n          console.log('✈️ AIRCRAFT RECEIVED:', aircraft);\n          this.updateAircraft(aircraft);\n        });\n        // =========================\n        // 🚨 ALERT STREAM\n        // =========================\n        this.stompClient.subscribe('/topic/alerts', message => {\n          const alert = JSON.parse(message.body);\n          console.log('🚨 ALERT RECEIVED:', alert);\n          this.updateAlert(alert);\n        });\n        // =========================\n        // 📊 STATS STREAM\n        // =========================\n        this.stompClient.subscribe('/topic/stats', message => {\n          const stats = JSON.parse(message.body);\n          console.log('📊 STATS RECEIVED:', stats);\n          // ✅ UPDATE DASHBOARD DIRECTEMENT\n          const activeAlerts = stats.activeAlerts ?? 0;\n          this.activeAlertsSubject.next(activeAlerts);\n          this.statsSubject.next({\n            aircraftCount: stats.totalAircrafts ?? 0,\n            averageSpeed: Math.round(stats.averageSpeed ?? 0),\n            alertCount: activeAlerts\n          });\n        });\n      };\n      this.stompClient.activate();\n    }\n    loadInitialAlerts() {\n      this.http.get('http://localhost:8084/all-allerts').subscribe({\n        next: alerts => {\n          console.log('✅ ALERTS LOADED:', alerts.length);\n          this.alertsSubject.next(alerts);\n        },\n        error: err => {\n          console.error('❌ Error loading alerts:', err);\n        }\n      });\n    }\n    // =========================\n    // ✈️ UPDATE AIRCRAFT\n    // =========================\n    updateAircraft(aircraft) {\n      // ✅ BACKEND ENVOIE latitude/longitude\n      if (!aircraft || aircraft.latitude == null || aircraft.longitude == null) {\n        console.warn('❌ Invalid aircraft ignored:', aircraft);\n        return;\n      }\n      // ✅ MAPPING BACKEND → FRONT\n      const mappedAircraft = {\n        ...aircraft,\n        lat: aircraft.latitude,\n        lon: aircraft.longitude,\n        speed: aircraft.velocity ?? aircraft.speed ?? 0,\n        country: aircraft.originCountry ?? aircraft.country ?? 'Unknown'\n      };\n      const current = this.aircraftSubject.value;\n      const exists = current.find(a => a.id === mappedAircraft.id);\n      let updated;\n      if (exists) {\n        updated = current.map(a => a.id === mappedAircraft.id ? mappedAircraft : a);\n      } else {\n        updated = [...current, mappedAircraft];\n      }\n      this.aircraftSubject.next(updated);\n      this.trafficHistorySubject.next(this.rollHistory(updated.length));\n      console.log('✅ AIRCRAFT COUNT:', updated.length);\n    }\n    // =========================\n    // 🚨 UPDATE ALERT\n    // =========================\n    updateAlert(alert) {\n      if (!alert) return;\n      const current = this.alertsSubject.value;\n      const exists = current.find(a => a.id === alert.id);\n      if (exists) {\n        const updated = current.map(a => a.id === alert.id ? alert : a);\n        this.alertsSubject.next(updated);\n      } else {\n        this.alertsSubject.next([alert, ...current]);\n      }\n    }\n    // =========================\n    // 🌍 COUNTRY ANALYTICS\n    // =========================\n    buildCountryTraffic(aircraft) {\n      const countryMap = {};\n      aircraft.forEach(a => {\n        const country = a.country || 'Unknown';\n        countryMap[country] = (countryMap[country] || 0) + 1;\n      });\n      return Object.entries(countryMap).map(([country, count]) => ({\n        country,\n        count\n      })).sort((a, b) => b.count - a.count);\n    }\n    // =========================\n    // 📈 HISTORY\n    // =========================\n    rollHistory(current) {\n      const prev = this.trafficHistorySubject.value.slice(-9);\n      return [...prev, current];\n    }\n    // =========================\n    // 🧹 CLEANUP\n    // =========================\n    disconnect() {\n      this.stompClient.deactivate();\n    }\n    static {\n      this.ɵfac = function AircraftService_Factory(t) {\n        return new (t || AircraftService)(i0.ɵɵinject(i1.HttpClient));\n      };\n    }\n    static {\n      this.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n        token: AircraftService,\n        factory: AircraftService.ɵfac,\n        providedIn: 'root'\n      });\n    }\n  }\n  return AircraftService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}